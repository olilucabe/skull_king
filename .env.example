# Copia este archivo a .env y rellena los valores

# Clave secreta de Flask (cámbiala por una cadena aleatoria larga en producción)
SECRET_KEY=cambia-esto-por-algo-secreto

# Configuración del servidor (solo se usa con `python app.py`)
HOST=0.0.0.0
PORT=5000
DEBUG=false

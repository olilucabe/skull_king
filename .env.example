# Copy this file to .env and fill in the values

# Flask secret key (change this to a long random string in production)
SECRET_KEY=change-this-to-something-secret

# Server configuration (only used when running `python app.py` directly)
HOST=0.0.0.0
PORT=5000
DEBUG=false
